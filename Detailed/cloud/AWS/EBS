An EBS (Elastic Block Store ) Volumns is a network drive oyou can attach to yuor instnaces while they run

it allows your instances to persist datam eveb after their termination '
they can only be mounted to one instance at a tiem 
they are boudn to a specific volumn

its network drive (not physical drive )
it can be detached.
its locked to an avialblity zone however we can do it via snapshot.

Have a provisioned capcity (size in FBs and IOPS)

EBS volume - Example 
 [ US-EAST-1A ]


EBS Hands on - 
EBS snapshots - is the backup - we can copy the snapshot to ddfirent zones
 
